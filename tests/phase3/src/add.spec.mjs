import assert from 'assert'
import add from './add'

describe('add', () => {
  it('can add two numbers', () => {
    assert.equal(add(1, 2), 3)
  })

  it('can add two numbers', () => {
    assert.equal(add(1, 2), 3)
  })
})
